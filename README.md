# How to use:
* This repository is created to attach a .mdf or .bak of SQL Server to the SQL Server Express edition using Python to the ECS Container.
* Additionlly it can be integrated to any of the step function in AWS which has ECR repository, ECS cluster and ECS Task definition defined already.
